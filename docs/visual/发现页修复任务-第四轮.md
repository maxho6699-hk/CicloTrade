# 发现页修复任务（第四轮 · 待执行）

> 用户验收满意后提出的两个改进点：
> 1. **中栏占满屏幕**：中间区域底部有空白，候选表+事件时间轴应撑满剩余垂直空间。
> 2. **响应式三端**：手机/平板/桌面都要好看（手机 390px、平板 1024px、桌面 1920px）。

## 修复内容

### 1. 中栏纵向占满
- 让 `.discover-center-column` 内的候选矩阵卡 + 事件时间轴卡 `flex-grow` / `grid-template-rows: 1fr` 撑满视口剩余高度。
- 参考：`.discover-dashboard` 已设 `min-height: calc(100vh - topbar - status)`，中栏卡片应 stretch。
- 效果：1920x1080 下候选表底部不再有空白，时间轴贴底。

### 2. 三端响应式验收
- 手机 390px：三栏变单列卡片流；候选表每行变卡片；AI 横幅压缩；KPI 4→2 列；触控 ≥44px；底部留导航空间。
- 平板 1024px：右栏变抽屉或两列；侧边导航收起。
- 桌面 1920px：完整三栏。
- 无横向滚动、无遮挡、无大面积空白。

## 执行方式
- 只改 `src/apps/web/src/styles/today-discover-v2.css` + `src/apps/web/src/pages/DiscoverV2Page.tsx`（如需要加 wrapper class）。
- 改完 build + test 通过。
- 验收：三端截图（390/1024/1920）视觉模型核对。

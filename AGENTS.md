# TradeAI 视觉重做工作区（临时覆盖指令）

## 本工作区任务模式

**当前任务 = 前端视觉重做样板页。执行模式与主仓库常规开发不同，特此覆盖：**

1. **禁止启动 Ruflo swarm / 多智能体协调**。禁止调用 `spawn_agent`、`wait_agent`、`send_message`、`list_agents`、`mcp__ruflo*` 等任何 swarm 或协调工具。
2. **你是直接实现者**：读规范 → 改代码 → 跑构建 → 输出结果，一步到位，不要先派 agent 调查。
3. 只改任务指定的文件（见任务指令），禁止改动其他页面和业务逻辑。
4. 业务/安全边界（账户域隔离、数据真实性、AI 不下单）必须保留，但**视觉与布局以 `docs/visual/` 下三份规范为准**，旧设计合同的视觉约束已被覆盖。

## 规范优先级（本工作区）

1. `docs/visual/视觉风格授权规范.md`（用户想要的视觉风格，最高优先级）
2. `docs/visual/布局规范文档-唯一执行指令.md`（布局骨架）
3. `docs/visual/页面功能布局映射规范.md`（功能→区域映射）
4. 其他所有旧文档（含 `docs/rewrite/*` 设计合同）仅作业务参考，视觉/布局约束不适用。

## 完成任务的标准

- 改完代码后运行 `npm run build`（`src/apps/web`）通过。
- 运行 `npm run test:unit` 不挂。
- 在最终回复里说明：改了哪些文件、骨架如何对应规范、build/test 结果。
